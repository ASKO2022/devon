db:
  image: {{DB_IMAGE}}
  container_name: {{PROJECT}}-db
  environment:
    POSTGRES_USER: {{DB_USER}}
    POSTGRES_PASSWORD: {{DB_PASSWORD}}
    POSTGRES_DB: {{DB_NAME}}
  volumes:
    - {{PROJECT}}_dbdata:/var/lib/postgresql/data
  ports:
    - "{{DB_PORT}}:{{DB_PORT}}"
